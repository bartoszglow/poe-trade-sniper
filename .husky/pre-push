pnpm format:check
pnpm audit --audit-level=high
if command -v gitleaks >/dev/null 2>&1; then
  gitleaks git --no-banner --redact .
else
  echo "ERROR: gitleaks is not installed (brew install gitleaks) — refusing to push unscanned." >&2
  exit 1
fi
