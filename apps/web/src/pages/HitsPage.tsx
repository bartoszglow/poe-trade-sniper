export function HitsPage() {
  return (
    <section>
      <h1 className="text-lg font-semibold text-ink">Hits</h1>
      <p className="mt-6 text-sm text-ink-faint">
        Full detection history with expandable item details — searchable and filterable. The live
        stream lands in the right panel (Phase 3); this page adds the persisted history from the
        hits table.
      </p>
    </section>
  );
}
