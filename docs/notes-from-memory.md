# Notes relocated from Vault memory (2026-06-22)

> Project knowledge + working rules moved here from the Vault Claude-memory during the 2026-06 restructure. Authoritative going forward — promote key rules into CLAUDE.md as you see fit.

---

---

name: project_poe_trade_sniper_rewrite
description: "poe-trade-sniper — rewrite wg card-bridge. Tip 9fe00d9 (68 commitów, working tree clean, 102 unit + 10 e2e green), NO REMOTE/push. Fazy 0-5 + i18n + WS persistent-tab model + Network debug view + Hits overhaul + in-app update check SHIPPED 2026-06-12/13. DMG działa na :3580. Next: live-test WS (1013 znika?) + remote/CI + cutover."
metadata:
node_type: memory
type: project
originSessionId: 70647f45-a20a-48cd-8fb2-35b984c8b04c

---

**poe-trade-sniper** — przepisanie od zera prototypu [[project_poe2_live_sniper]] według standardów engineeringowych **card-bridge**. Decyzja 2026-06-11: NOWY równoległy projekt `~/Workspaces/poe-trade-sniper` (repo jeszcze NIE istnieje).

**KRYTYCZNE: stary prototyp `~/Workspaces/poe2-live-sniper` działa i Bartosz go używa codziennie — NIE DOTYKAĆ.** Serwer chodzi w tle na `http://localhost:3411` (odpalany `pnpm start` z repo prototypu). Cutover dopiero w Phase 5.

**Master plan**: `Vault/Projects/Poe-Trade-Sniper/00_master_plan.md` — przeczytaj PRZED pracą. Zawiera: architekturę modułów NestJS, schemat DB, strategię hybrid web→desktop, fazy 0-6, decyzje, §10 hard-won knowledge (w tym no-guessing rule → `docs/integration/api-notes.md`, WS tarpit, tooling gotchas). Satelity (2026-06-12): `10_phase0_plan.md` (szczegółowy plan Phase 0 z execution order + acceptance checklist), `30_open_questions.md`, `40_decisions.md`, `90_future_ideas.md`. Draft `rewrite_plan.md` USUNIĘTY (zmergowany do master planu).

**Decyzje 2026-06-12 (review przed startem):** D-6 = **better-sqlite3** (nie libsql; electron-rebuild dopiero w Phase 5). D-7 = sesja plain file `0600` do Phase 4, ale interfejs `SessionStore` od Phase 1. Phase 1 bootstrapuje sesję importem JSON-a z prototypu + nagrywa fixtures trade-API póki prototyp żyje.

**Stack locked (Bartosz wybrał 2026-06-11):**

- Backend: **NestJS** (Node 22, TS strict) — moduły: config(Zod fail-fast)/db/session/trade-api/search/engines/travel/ratelimit/events/items/api
- Frontend: **React 19 + Vite + Tailwind** — shell + nav registry, atomic components (enumy nie booleany), SSE
- Desktop: **Electron** (Win/Linux/macOS) — hybrid "one server, two shells"; NestJS w main process; login do PoE przez BrowserWindow (koniec walki z Cloudflare/Playwright)
- DB: **SQLite + Drizzle** embedded (plik, JSON columns na item payload, forward-only migrations) — tabele: searches/hits/app_state
- Repo: **pnpm monorepo** — apps/server + apps/web + apps/desktop + packages/shared + e2e + docs/
- Testy: Vitest + Playwright; quality gates jak card-bridge (verify=lint+typecheck+test, husky, CI, CHANGELOG)

**Wzorzec standardów**: `~/Workspaces/card-bridge/docs/` (architecture.md = kontrakt registry, conventions.md, code-review.md z rubryką S1-S4, frontend.md) + jego CLAUDE.md. Scope cuts vs card-bridge: BEZ RBAC/PCI-masking/i18n/NDA — bierzemy dyscyplinę, nie bankową maszynerię.

**Kluczowe zasady projektu**: engine registry open/closed (ws/poll, nowy engine = append do tablicy); trade-api = JEDYNY moduł gadający z pathofexile.com (header discipline w jednym miejscu); ratelimit governor czyta żywe X-Rate-Limit-\* headers (nie hardcode); correlation id przez search→fetch→travel; sesja PoE = secret (nie logować, nie zwracać do UI); auto-travel = explicit per-search opt-in.

**Phase 0 SHIPPED 2026-06-12** — 10 commitów lokalnie w `~/Workspaces/poe-trade-sniper` (fda282f → ab5478e). `pnpm verify` + e2e green. Wersje: Node 22.22 / pnpm 11.5 / TS 6.0 / ESLint 10 / NestJS 11 / Zod 4 / React 19 / Vite 8 / Tailwind 4 / Drizzle + better-sqlite3 / Vitest 4 / Playwright 1.60. Endpoints pod global prefix `/api`; web = relative URLs (Vite proxy dev :5180 → :3500). pnpm 11 gotcha: `allowBuilds` w pnpm-workspace.yaml (esbuild, better-sqlite3).

**Shell design locked (D-8, mockup approved 2026-06-12):** icon rail + PERSISTENT live-hits panel (prawa strona, lg+) + app bar (frameless-ready przez `data-shell="web|desktop"` + `.app-drag-region` CSS) + status bar. PoE theme: near-black + gold tokens w `@theme` (index.css), compact 14px. Repo docs: `docs/integration/api-notes.md` = evidence log GGG API.

**WAŻNE: brak remote** — Bartosz sam utworzy GitHub repo; NIE tworzyć repo / NIE pushować bez jego polecenia. CI workflow czeka w `.github/workflows/ci.yml`.

**Stary prototyp**: nie działa już na :3411 (zweryfikowane 2026-06-12, brak procesu) — Bartosz kazał go zamknąć; restart przez `pnpm start` w `~/Workspaces/poe2-live-sniper` gdyby chciał go wskrzesić do czasu parity.

**Phase 1 SHIPPED 2026-06-12** (`765a31d → dbac6cd`, plan `Vault/.../20_phase1_plan.md`): trade-api adapter (jedyny gateway GGG), ratelimit governor (żywe X-Rate-Limit headers, 429 = global pause, near-cap hold), engines registry ws/poll (tarpit-guarded probe, auto-upgrade poll→ws co WS_RECONNECT_MAX_MS), SearchManager (round-robin 1 search-POST/tick, hits persisted, baseline round), session module (cookie paste POST /api/session/cookies + `pnpm session:import` z prototypu), purchase mode per search (TYLKO `instant`→`securable` verified; reszta TODO(verify) = O-5), RealtimeBus→SSE `/api/events`. 66 unit + 7 e2e green.

**KRYTYCZNE gotchas:** (1) D-11: tsx/esbuild NIE emituje decorator metadata → KAŻDY param konstruktora Nest musi mieć explicit `@Inject(Token)`; unit testy tego nie łapią, boot smoke tak. (2) D-10: GGG OAuth niedostępny dla trade API — tylko cookie session; cookie paste = trust-friendly path (Bartosz explicite chciał). (3) hideoutToken NIE jest persystowany w hits (TTL ~300s).

**Phase 2 SHIPPED 2026-06-12** (`0339ac9 → b6ca095`, plan `21_phase2_plan.md`): trade-api `travel()` (whisper POST z X-Requested-With + Referer, policy `whisper`), TravelService FIFO one-at-a-time (stale > 240s dropped, D-13: no retries/no browser fallback), auto-travel = subscriber hit eventów na RealtimeBus (gate: autoTravel flag + hideoutToken), manual `POST /api/travel`, `travel` lifecycle events na SSE, travel w `/api/status`. 74 unit + 8 e2e green.

**D-12 (Bartosz, ważne dla Phase 3):** Settings UI musi mieć OBA paths logowania: "Log in with PoE" (in-app, realna strona GGG) ORAZ cookie paste — żaden nie jest fallbackiem.

**Phase 3 SHIPPED 2026-06-12** (`800e80e → 3507b9b`, plan `22_phase3_plan.md`): EventStreamProvider (1× EventSource, live hits cap 100, travel states per listing, searchesVersion → refetch), useServerStatus (poll 10s), Searches page (add form + inline purchase-mode/AUTO PATCH + two-step delete), HitsPanel live (HitCard z token-gated Travel, freshness tick 30s, mirror 240s), HitsPage (filter + ItemDetailView accordion), Settings (probe/verify + cookie paste z auto-probe + DISABLED "Log in with PoE" button "phase 4/5" per D-12 + rate-limit budgets), status bar (session dot / search budget / travel queue / rate-limited until). Nowy endpoint `POST /api/session/probe`. Atomy: Field/TextInput/Select/Switch/IconButton + RarityName/PriceTag/ItemDetailView/HitCard. 74 unit + 9 e2e green.

**LIVE SESSION 2026-06-12 (w toku):** sesja prototypu zaimportowana i VALID (probe 200); **WS BACKEND DZIAŁA ZNOWU** (po patchu ~2026-06) — search Bartosza "test spear" (jW6LVr5lCX, Standard, AUTO on, max 400) chodzi na ws·active; league endpoint VERIFIED (`/api/trade2/data/leagues` → 4 ligi: Runes of Aldur/HC/Standard/Hardcore) → cached `GET /api/leagues`; ws reconnect ladder 1s/5s/20s/60s + close-code w statusDetail (d368826); **D-14: URL = source of truth** — purchase select UKRYTY w UI (backend override zostaje), league select tylko dla bare id (e8877a3); O-5 (4 mappingi) zdegradowane priorytetem. UI visual pass via /chrome OK. Czeka: pierwszy hit + realny auto-travel (background watcher), fixtures.

**Phase 5a (preliminary desktop) SHIPPED 2026-06-12:** apps/desktop Electron shell — embedded server w main process + loopback one-origin (`STATIC_DIR`/ServeStatic), D-15: **Electron pinned major 41** (ABI 145 = ostatni z better-sqlite3 prebuilds; 42 nie kompiluje się), dev mode `SNIPER_DEV_URL`→Vite (full HMR, zero ABI), preview = `abi:electron` swap (skrypt `apps/desktop/scripts/sqlite-abi.mjs`; **`abi:node` przed unit testami po preview!**). App odpalony i działał z live searchem.

**D-16 OutboundGuard (KRYTYCZNE, po incydencie):** ws ladder resetował się po KAŻDYM udanym open → connect→drop loop młócił 1s reconnecty (Bartosz złapał). Fix: reset ladder tylko po stabilnym połączeniu ≥60s (`WS_STABLE_CONNECTION_MS`). Plus watchdog: `guard/OutboundGuard` — ceilingi 90 HTTP/min + 12 ws-connects/min (config GUARD\_\*), trip = total halt (trade-api odmawia, engines wound down, czerwony GuardBanner w UI, `POST /api/guard/reset` manual only, reset czyści okna). Diagnoza live: GGG zamyka sockety z **code 1013 "Try Again Later"** — server-side backoff; ladder + guard to obsługują.

**Backlog batch SHIPPED 2026-06-12 noc (bc9f445/1ef31a2/5c4a4de, tip 5c4a4de, 40 commitów, 83 testy green):** ws→poll demotion po `WS_DEMOTE_AFTER_FAILURES`=3 niestabilnych cyklach (EngineCallbacks.onDemote → manager startuje fallback z registry, upgrade-probe wraca na ws po 2 min), close 1013 → od razu top rung drabinki, hits pruning (`HITS_MAX_ROWS`=10k, prune na boot + co 100 hitów), hit sound (WebAudio synth, localStorage toggle w Settings + Test button odblokowujący audio), SessionBanner (żółty, probedValid===false). GGG zamykał sockety 1013 "Try Again Later" = cooldown po incydencie 1s-loop.

**Stan procesów:** Bartosz usunął searcha podczas cooldownu (searches=0, zero ruchu GGG); dev stack 3500/5180 wstał z nową ochroną; sniper Electron ubity (UWAGA: nie używać szerokiego `pkill -if electron` — matchuje Slack/VSCode crashpady).

**Phase 4 + SELF-REVIEW SHIPPED 2026-06-12 (93a8e7f → 9a00750, tip 9a00750, 44 commity, 84 unit + 10 e2e green):** (a) sesja AES-256-GCM at rest, klucz w macOS Keychain (`SessionCipher`, injectable keyLoader w testach — NIGDY nie dotykać realnego keychaina w testach), legacy plaintext czytany + re-encrypt on save, D-7/O-1 zamknięte; (b) in-app login web: `LoginCaptureService` — spawn realnego Chrome (config `CHROME_BINARY`/`LOGIN_PROFILE_DIR`, dedykowany profil, random debug port), CDP `Storage.getCookies` poll co 5s, gate = probe `/my-account` (guest POESESSID nie przechodzi), endpoints `/api/session/login/{start,cancel}` + GET, Settings UI z pollingiem; (c) boot probe sesji po 3s; (d) SECURITY z review: `HOST`=127.0.0.1 bind, HostGuardMiddleware (DNS rebinding), Electron sandbox/contextIsolation + deny window.open/navigate, e2e na porcie 3590 bez reuse (e2e WIPE'OWAŁO realną sesję przez reuse!). Review record w repo: docs/process/reviews/2026-06-12-self-review.md (parked: brak auth API [loopback accepted], CDP port okno 5min, search-manager ~470 linii split-candidate, cookie-header dup).

**Rekomendacja dla Bartosza:** usunąć plaintext `~/Workspaces/poe2-live-sniper/session-state.json` (sesja już zaimportowana i zaszyfrowana).

**Phase 5 FULL SHIPPED 2026-06-12 (tip eae9c11, 48 commitów):** frameless (titleBarStyle hiddenInset, preload.cjs ustawia data-shell=desktop → drag region + traffic-light padding 4.75rem), serwer zbundlowany esbuildem do 1 pliku CJS (działa dzięki D-11 — zero decorator metadata; externals: better-sqlite3 + nest optionals + @fastify/static), `pnpm --filter @poe-sniper/desktop dist` → **DMG arm64 zweryfikowany działający** (health+migracje z extraResources+UI). MIGRATIONS_DIR env override w migrate.ts. GOTCHA: electron-builder przebudowuje better-sqlite3 in-place → po `dist` ZAWSZE `abi:node` przed testami. bundle/ i release/ w gitignore+eslint ignores. Deferred: signing/notarization, tray, auto-update, Win/Linux.

**UX BATCH 2026-06-12 rano (po feedbacku Bartosza, tip = ostatni commit "pointer cursor"):** (1) system notifications na hit + Test button (web Notification API → natywne w Electronie; toggle `sniper.systemNotifications` w localStorage, lib/notifications.ts); (2) themed scrollbary (::-webkit-scrollbar + scrollbar-color, index.css); (3) FIX traffic-light overlap: padding-left 84px w PX (rem był błędem — root font 14px!); (4) LoginOverlay na boot: brak sesji → "Not logged in", probe failed → "Your PoE session expired"; przycisk loginu + link do paste + X close; wspólny hook `useLoginCapture` (Settings + overlay); (5) cursor: pointer globalną regułą CSS (button/select/[role=switch]:not(:disabled)). DMG przebudowany z poprawkami (apps/desktop/release/), abi:node przywrócony po dist.

**Pytanie Bartosza o autofill haseł w login-Chrome:** to dedykowany PUSTY profil (celowo) — brak keychain/sync; opcje: hasło raz (profil trwały, GGG pamięta), rozszerzenie iCloud Passwords w tym profilu, albo paste cookies. Wyjaśnione, zaakceptowane na razie.

**Stan procesów na koniec sesji:** web dev stack 3500/5180 działał (0 searchów = 0 ruchu GGG po 1013-cooldownie); spakowana appka mogła być otwarta na :3580 (własna baza userData, bez sesji). Sesja w dev DB: zaimportowana + ENCRYPTED + probe valid.

**BATCH 2026-06-12 popołudnie (5 commitów: 2bb355f→ostatni "ACTIVE toggle"):** (1) **i18n EN/PL pełne** wg wzorca card-bridge: `apps/web/src/i18n/{messages.ts,i18n.ts,I18nProvider.tsx}` — EN as const = MessageKey source of truth, PL compiler-checked `Record<MessageKey,string>`, plurals via Intl.PluralRules (`searches.hitCount` one/few/many), `translateStatic()` dla kodu poza React tree (SSE→notyfikacje), storage `sniper.language`, default en + auto-detect pl; select języka w Settings (card "Language" na górze); nav registry: `label` → `labelKey`; server-supplied detail strings (statusDetail/ApiError/loginDetail) celowo NIE tłumaczone. Reguła w docs/architecture/frontend.md: KAŻDY nowy user-facing string = klucz w katalogu EN+PL. (2) **Volume slider** (Alerts card): WebAudio peak gain 0.18 × volume%, storage `sniper.hitSoundVolume`, atom `components/Slider.tsx` (native range + accent-gold; appearance-none NIE — ubija thumb), preview na release, disabled gdy sound off; notyfikacje systemowe są silent:true → slider rządzi całym audio. (3) Toggle per-search "AUTO"→**"TRAVEL"** (PL "PODRÓŻ"). (4) **Auto-travel dedup** (fix(server)): TravelService pamięta successfully-traveled listingIds (bounded Set, `TRAVEL_DEDUPE_MAX_ENTRIES`=500) — listing wraca do streama jako "nowy" gdy gracz wróci do HO bez kupna; auto-skip, manual zawsze OK, failed nie zapamiętany (retry możliwy); in-memory only (restart = 1 możliwy extra travel, accepted). (5) **ACTIVE toggle per-search** (pause bez usuwania): persisted `enabled` column (migracja 0003 via drizzle-kit generate), update(enabled:false) → engine stop + status stopped/'paused' + zero ruchu; enable → restart przez registry probe; paused bootuje jako stopped; PATCH /api/searches/:id {enabled}; Switch ACTIVE/AKTYWNY przed TRAVEL w SearchRow. 90 unit + 10 e2e green.

**DMG przebudowany 2026-06-12 i PODMIENIONY** (`apps/desktop/release/` — jedyna lokalna kopia, brak w /Applications): zawiera i18n+volume+pause+dedup+cursor fix; appka odpalona i zweryfikowana (health ok, dbMigrated:true = migracja 0003 w userData DB).

**GOTCHA REWIZJA (commit 82eebab):** electron-builder's @electron/rebuild potrafi cicho NIE przebudować better-sqlite3 i spakować node-ABI binarkę do app.asar.unpacked → spakowana appka umiera na boot z `NODE_MODULE_VERSION 127 vs 145` (pierwszy dist dzisiaj tak padł; wcześniej działało bo workspace był akurat w electron ABI). FIX: `dist` script robi teraz jawnie `abi:electron` PRZED electron-builderem i `abi:node` PO — drzewo zawsze test-ready, ręczne abi:node po dist już niepotrzebne. Debug spakowanej appki: odpalić binarkę z `Contents/MacOS/` bezpośrednio i czytać stdout.

**DRUGI GOTCHA dist (commit e9e7006):** extraResources pakuje `apps/web/dist` VERBATIM — `dist` NIE budował weba, więc DMG cicho shipował starą wersję UI (Bartosz złapał: brak language select w świeżej paczce). FIX: `dist` = `abi:electron && pnpm --filter @poe-sniper/web build && build && bundle:server && electron-builder && abi:node` — teraz jedna komenda robi wszystko od zera.

**BATCH 2026-06-12 (commits 5916b24 + a4a61fd):** (1) **Custom Select** (port card-bridge): listbox zamiast native <select> (OS dropdown ignorował dark theme), zamyka się po wyborze, keyboard nav, click-outside, gold check; API `onChange(value)`; gotcha React compiler: ZERO `optionRefs.current = []` w renderze (nawigacja bounded przez options.length, stale refs nieosiągalne). (2) **Criteria view** (zaakceptowany mockup): `QueryCriteriaView` + parser `lib/query-criteria.ts` (cardinal rule: NIGDY nie ukrywać danych — nierozpoznane klucze → raw JSON), stat id→label przez `GET /api/stats` (server cache 24h, `STATS_CACHE_TTL_MS`, wzorzec leagues) + module-level cache w `useStatsDictionary`; akordeon na SearchRow (filters już płyną z GET /api/searches — zero kosztu GGG) + "Show criteria" w add form przez `POST /api/searches/preview` (resolve bez persystencji, waliduje input przed dodaniem). Research note: bare search ID = opaque klucz do bazy GGG (jak URL shortener), nie szyfr; resolve = GET search/<realm>/<league>/<id>, generacja = POST query (GGG nadaje slug).

**BATCH 2026-06-12 wieczór (commits e5fa6b1 + af57627 + c4e25b1):** (1) **Criteria view redesign**: responsywne karty-grupy (grid 1→2→3 kol `sm:grid-cols-2 xl:grid-cols-3`) zamiast ciasnego label/rows; parser fix — `null` bounds traktowane jak brak (`gem_sockets {max:0,min:null}` → "max 0" nie "min null"); cena jako range "≤ 100 [waluta]", waluta z `price.option` GDY jest (w realnym query Skysliver `price:{max:100}` BEZ waluty — nie wymyślamy). Ground truth query wyciągnięty z live API `:3580/api/searches`. (2) **WS shared monitor refactor** (zatwierdzony przez Bartosza): backend live up/down jest GLOBALNY → JEDEN wspólny probe na własnym timerze (`WS_UPGRADE_PROBE_INTERVAL_MS`), OFF poll ticka (slow handshake nie zacina polla); sukces → flaga → promocja WSZYSTKICH poll searchy naraz w następnym ticku (synchronicznie, bez race), dławiona przez nowy `guard.wsConnectBudgetRemaining()`. WS drop: stabilne (≥60s) → 1 szybki reconnect (jak realna strona); niestabilne LUB **1013** → natychmiastowy demote na poll (koniec ciemnej drabinki 60s). Usunięte: `upgradeOnePollWatcher`, per-watcher `lastWsUpgradeProbeAtMs`, `WS_DEMOTE_AFTER_FAILURES`, `reconnectAttempt`. **Architektura potwierdzona: JEDEN socket per search** (endpoint GGG `trade2/live/<id>` jest per-search-id, brak multipleksowania — realna strona robi tak samo, N zakładek = N socketów). `probeLiveBackend()` public dla testów. 4 nowe testy (decoupling/promote-all/budget-defer/demote-repromote), 94 unit total. (3) **Custom Select** (5916b24, wcześniej): listbox port card-bridge, zamyka po wyborze. (4) **WS/POLL pills w app barze** (c4e25b1): globalna postawa detekcji, derived z live searches listy (useSearches w AppShell, refetch na engine-status SSE → śledzi demote/repromote real-time), ukryte gdy 0 enabled searchy.

**DMG przebudowany 2026-06-12 wieczór i działa** (criteria cards + WS monitor + pills, health ok, assets fresh).

**BATCH 2026-06-12 noc — WS model rewrite + debug (commits 6da780c → d0f4c0d):** (1) **Niezależny scroll kolumn** (6da780c): grid row `1fr`→`minmax(0,1fr)` + `min-h-0`/own-overflow na main+aside+HitsPanel list (`shrink-0` header) — wysoki content nie rozpychał gridu ponad h-screen, nie scrolluje już body/menu. (2) **Fix licznika hitów** (7e3ab5c): watcher.hitCount/lastHitAt były in-memory, zerowały się przy restarcie → search wyglądał martwo mimo działania. `hydrateHitStats()` ładuje COUNT+MAX(detectedAt) z tabeli hits przy bootstrapie (zweryfikowane: po rebuildzie pokazuje 53 hity). (3) **DEBUG „WS nie działa"**: diagnoza przez czytanie `:3580/api/searches` (bez uderzania GGG) — WS faktycznie łapał (hitCount rósł), ale **migotał WS↔POLL** przez 1013. Przyczyna: nasz churn połączeń (probe-connect + connect + demote/repromote) vs przeglądarka/prototyp = jedno trwałe połączenie. (4) **WS MODEL REWRITE** (d0f4c0d, zatwierdzony przez Bartosza): **jeden TRWAŁY socket WS per search** (jak zakładka przeglądarki) + **PollEngine pokrywa TYLKO lukę reconnectu**. WsEngine nigdy się nie poddaje — każdy close (też 1013) → reconnect po drabince `WS_RECONNECT_LADDER_MS` (1013→top rung), reset po stable; ZERO churnu (bez throwaway probe). Scheduler round-robin pollu tylko po watcherach z `!wsConnected`; ws active → pollEngine torn down (świeży per luka → baseline, brak dubli). Display engine = ws gdy connected, poll w luce. **Usunięte:** ws→poll demote, shared probe (`probeLiveBackend`/`promotePollWatchersToWs`/`WS_UPGRADE_PROBE_INTERVAL_MS`), `EngineFactory.probe` + `probeLiveWebSocket`, `guard.wsConnectBudgetRemaining`, `EngineCallbacks.onDemote`. 94 unit (3 nowe: poll-covers-gap / ws-drop-repoll / one-ws-per-search) + 10 e2e green.

**NETWORK VIEW shipped 2026-06-12 (commit 21890a3):** dev widok + udostępnialny zredagowany log requestów GGG. JEDEN sink `NetworkLog` karmiony z 2 istniejących choke pointów (`TradeApiClient.request` + `WsEngine`) → ring buffer (`GET /api/network`) + live `network` SSE event + rotujący JSONL plik w `LOG_DIR` (desktop → `~/Library/Logs/@poe-sniper/desktop/network/`, ZAWSZE pisany). **Redakcja STRUKTURALNA**: typ `NetworkLogEntry` NIE MA pola na cookie/UA/token — niemożliwe by wyciekło (zweryfikowane: 0 dopasowań sekretów w realnym pliku). Widok `/network`: live tabela (czas względny via `lib/relative-time.ts`, channel/method/endpoint/policy/status/duration/outcome, expand detail, filtry, pauza), wpis w nav `devOnly` chowany przełącznikiem Settings „Developer→Network view" (`hooks/useNetworkView.ts`, localStorage `sniper.networkView`, default ON dla dev). 98 unit + 10 e2e green. Config: LOG_DIR/LOG_MAX_BYTES/NETWORK_LOG_RING_SIZE.

**QUEUED FEATURES — status (user wybrał „wszystkie po kolei"):** (1) **live-hits travel UX** ✅ SHIPPED (commit 5905ca8): relative time „x temu" (HitCard, tick 5s) + manual RETRY przy failed travel (tokenFresh-gated). (2) **Hits view overhaul** ✅ SHIPPED (commit 89e92bf): paginowany/filtrowany/sortowany `GET /api/hits` (searchId/search/from/to/sort/limit/offset), HitsPage z free-text search + date range + sort select + **infinite scroll** (IntersectionObserver, 20/stronę, spinner Loader2) + **item-detail refactor na karty** przez wspólny `components/DetailCard.tsx` (DetailCard+DetailRow wydzielone z QueryCriteriaView, ItemDetailView przerobiony na grid kart). (3) **in-app updates** ✅ SHIPPED (commit 9fe00d9): lekki check (user wybrał: lekki + GitHub Releases). `GET /api/update` → UpdateService pyta `api.github.com/repos/<GITHUB_RELEASES_REPO>/releases/latest`, porównuje tag z APP_VERSION (`isNewerVersion` semver), zwraca {currentVersion, latestVersion, updateAvailable, releaseUrl, downloadUrl}, cache TTL. **Dormant dopóki `GITHUB_RELEASES_REPO` (config) puste** — ustawić gdy powstanie repo. UpdateBanner (info, w banners row AppShell) „nowa wersja → Pobierz"; Electron `setWindowOpenHandler` otwiera http(s) przez `shell.openExternal` (real browser). BRAK silent auto-update — wymaga signing+notarization (deferred). 102 unit + 10 e2e green. DMG przebudowany, `/api/update` zweryfikowany (dormant).

**TIP dla in-app updates gdy powstanie repo:** ustaw `GITHUB_RELEASES_REPO=owner/repo` (env lub desktop main.ts), publikuj GitHub Release z tagiem `vX.Y.Z` + assetem `.dmg`, bumpnij `apps/server/src/version.ts APP_VERSION`. Banner zapali się automatycznie.

---

**PRE-COMPACT SNAPSHOT 2026-06-13:** Tip `9fe00d9`, **68 commitów lokalnie, working tree CLEAN, NADAL BRAK REMOTE** (nie pushować — Bartosz sam zrobi repo). 102 unit + 10 e2e green, lint czysty. **Procesy:** desktop app DZIAŁA na :3580 (userData DB, search „Skysliver pJ66rWJbS0" 53+ hitów, ostatnio PAUSED przez Bartosza); web dev vite :5180 też chodzi (server-dev-watch nie). Sesja w dev/userData DB zaszyfrowana + ważna.

**Architektura serwera (modułowo, app.module.ts):** Config(Zod)/Db(drizzle+better-sqlite3, migracje 0000-0037+0003_search_enabled)/Events(RealtimeBus→SSE `/api/events`)/Network(NetworkLog sink)/Guard(OutboundGuard watchdog)/Session(AES-GCM+Keychain)/RateLimit(governor z żywych X-Rate-Limit)/TradeApi(JEDYNY gateway GGG, `request()` choke point)/Travel(FIFO+dedup)/Update(GitHub check)/Search(SearchManager+engine-registry)/Api. Endpoints pod `/api`: searches(+preview), hits(paginowane), leagues, stats, status, network, update, session/\*, guard/reset, travel, events(SSE), health.

**DETEKCJA — aktualny model (KRYTYCZNE, po 2 iteracjach):** JEDEN trwały WS socket per search (jak zakładka przeglądarki) + PollEngine pokrywa TYLKO lukę reconnectu. WsEngine nigdy się nie poddaje (każdy close, też 1013, → reconnect po drabince, reset po stable, ZERO churnu). Poll round-robin tylko po `!wsConnected`. Brak demote/shared-probe (usunięte). To NIE było jeszcze live-testowane po ostatnim rebuildzie — Bartosz ma re-enable search i sprawdzić czy 1013/migotanie znika.

**Najważniejsze gotchy (poza już udokumentowanymi D-11/ABI/dist):** dist robi web-build+abi-swap sam; `husky eslint --fix` bywa surowszy niż `pnpm lint` (type-aware no-unsafe-return) — sprawdzaj po commicie; messages.ts EN=source-of-truth, PL compiler-checked (brak klucza = build error); sekrety NIGDY w logach (NetworkLogEntry strukturalnie bez pola cookie/token).

**NEXT (priorytet):** (1) Bartosz re-enable search → live-test nowego WS modelu (1013 znika? migotanie?); (2) pierwszy realny auto-travel + fixtures do e2e; (3) test in-app login button (realny Chrome); (4) remote+push+CI gdy Bartosz zrobi repo; (5) cutover/archiwizacja prototypu `poe2-live-sniper`; (6) O-5 (4 purchase-mode status.option) opportunistycznie; (7) deferred: code signing+notarization → wtedy pełny auto-update.

**Next actions**: zacząć jedną z 3 queued features (czekam na wybór usera); + re-enable search → live-test WS modelu (1013 znika?); pierwszy auto-travel + fixtures; in-app login test; remote+push+CI; O-5; cutover.

Related: [[project_poe2_live_sniper]] (prototyp + cała wiedza domenowa o trade2 API).
